###############################################
# Global settings -> RTMP server
rtmp: yes
rtmpAddress: :1935
rtmpEncryption: "no"

###############################################
# Paths
paths:
  all:
    # Разрешаем публиковать/читать без авторизации на любые пути (упростим на старте)
    # В проде лучше включить auth.
    source: publisher

#  obs:
#    source: udp://127.0.0.1:10010
#    sourceOnDemand: yes      # опционально: поднимать при первом запросе

